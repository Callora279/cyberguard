apiVersion: v1
kind: Service
metadata:
  name: cyberguard-web
  labels: { app: cyberguard, component: web }
spec:
  selector: { app: cyberguard, component: web }
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP

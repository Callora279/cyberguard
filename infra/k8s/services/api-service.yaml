apiVersion: v1
kind: Service
metadata:
  name: cyberguard-api
  labels: { app: cyberguard, component: api }
spec:
  selector: { app: cyberguard, component: api }
  ports:
    - name: http
      port: 80
      targetPort: 8090
  type: ClusterIP
